apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "vehicle-info-web.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
spec:
  data:
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager-secret-store
  target:
    name: {{ include "vehicle-info-web.fullname" . }}-secret
    template:
      metadata: {}
